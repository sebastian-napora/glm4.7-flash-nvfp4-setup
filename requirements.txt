# GLM-4.7-Flash serving requirements
# Hardware: NVIDIA GB10 (Blackwell, CC 12.1), CUDA 13.0, torch 2.11.0

# ── Core inference ────────────────────────────────────────────────────────────
# vLLM 0.20.1+ requires torch==2.11.0 (same as SGLang 0.5.11)
# Install from the official vLLM wheel index:
#   pip install "vllm>=0.20.0" --extra-index-url https://wheels.vllm.ai/latest
vllm>=0.20.0
huggingface_hub>=0.20.0

# ── SGLang (EAGLE speculative decoding) ──────────────────────────────────────
# Install with cu130 flashinfer wheels:
#   pip install "sglang[all]==0.5.11" \
#       --extra-index-url https://flashinfer.ai/whl/cu130/torch2.9/
# On Blackwell (CC 12.x) flashinfer crashes under speculative decoding;
# use --attention-backend triton --speculative-draft-attention-backend triton
sglang[all]>=0.5.11

# transformers is pinned by sglang; install from HF main for GLM-4.7 support:
#   pip install git+https://github.com/huggingface/transformers.git
transformers>=5.0.0

# ── Proxy / API layer ─────────────────────────────────────────────────────────
litellm>=1.0.0
fastapi>=0.100.0
uvicorn[standard]>=0.20.0
aiohttp>=3.9.0

# ── Utilities ─────────────────────────────────────────────────────────────────
pypdf>=3.0.0