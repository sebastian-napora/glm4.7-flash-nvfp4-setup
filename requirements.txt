# GLM-4.7-Flash-NVFP4 serving requirements
# Reuse the lunch-model venv which already has compatible versions:
#   vllm>=0.14.0, transformers>=5.0.0, litellm, fastapi, uvicorn, aiohttp

# If you need a fresh install:
vllm>=0.14.0
transformers>=5.0.0
litellm>=1.0.0
fastapi>=0.100.0
uvicorn[standard]>=0.20.0
aiohttp>=3.9.0
huggingface_hub>=0.20.0